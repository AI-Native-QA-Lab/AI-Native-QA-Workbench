# Security Policy

Do not commit API keys, access tokens, passwords, credentials, or other
secrets.

Project files under `.ai-qa/` must contain only non-secret
project-quality information.

Provider credentials should use environment variables or a future
OS-native secret store.

Security issues should not be disclosed through public issue content
containing exploitable secrets or credentials.
